
IP camera name: <%= @vivocamera.name  %> ( <%= link_to "back" , root_path  %>)
<hr />

<%= form_for @userinput, :url => vivocamera_userinputs_path(@vivocamera)   do |f|  %>
    <%= render 'common1', :f =>f %>
    <%= f.submit "Create" %>
<% end  %>